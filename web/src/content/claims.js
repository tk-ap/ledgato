export const claims = {
  product: 'Independent authorization and enforcement for consequential AI actions.',
  hero: 'Ledgato independently authorizes consequential agent actions at protected boundaries, then verifies and records what happened.',
  heroProof: 'When a protected action is routed through Ledgato, DENY means the downstream action does not execute.',
  connect: 'Connect the agents, tools, identities, and protected actions you want Ledgato to govern.',
  discover: 'Within connected integrations, compare what an agent can reach with the authority it was actually given.',
  declare: 'Scope as code. fence.yaml fixes what each agent may reach, and what needs approval.',
  verify: 'Attack and regression-test connected boundaries. Failed or drifted checks can gate the protected workflow before release.',
  enforce: 'Interpose ALLOW / DENY / APPROVE before a protected downstream action is invoked.',
  prove: 'Attach signed, tamper-evident evidence to every decision — an offline report an auditor can verify.',
  requestAccess: "Tell us which agent workflow and consequential action you want to place behind a boundary. We'll map the route, test bypasses, and define what is actually enforced.",
  appStatus: 'ALVIRA-connected data is shown only when the engine is reachable. This view distinguishes engine-derived state from unavailable or user-entered workspace data.'
}

export const statusLabels = {
  demo: 'DEMO · SAMPLE ENVIRONMENT',
  connected: 'CONNECTED · ENGINE-DERIVED',
  unavailable: 'ALVIRA · CONNECTION STATUS',
  userEntered: 'USER-ENTERED WORKSPACE'
}
